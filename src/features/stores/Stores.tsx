import React from 'react';
import StoresList from './components/StoresList';

const Stores = () => {
    return (
        <div>
            <StoresList />
        </div>
    );
};

export default Stores;
